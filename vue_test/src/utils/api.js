// 导入 axios
import axios from 'axios'

// url基础路径
let base = '';

export const getRequest = (url, parmas) => { }
export const postRequest = (url, params) => { }
export const putRequest = (url, params) => { }
export const deleteRequest = (url, params) => { }